name: Daily AI Post

on:
  schedule:
    - cron: "10 6 * * *" # Runs at 06:10
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Content generated locally
        run: echo "Content is generated locally via daily-post.sh on Joe's Mac. No action needed here."
